{
	"extends": "./base.json",
	"name": "go-template (with Docker socket)",

	"features": {
		"ghcr.io/devcontainers/features/docker-outside-of-docker:1": {}
	},

	"mounts": [
		"type=bind,source=/var/run/docker.sock,target=/var/run/docker.sock"
	]
}
